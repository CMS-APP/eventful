import { StyleSheet } from "react-native";

export const padding = StyleSheet.create({
  largeWidget: {
    alignItems: "center",
    borderRadius: 24,
    padding: 20
  },
  mediumWidget: {
    alignItems: "center",
    borderRadius: 12,
    padding: 12
  },
  smallWidget: {
    alignItems: "center",
    borderRadius: 12,
    padding: 6
  }
});
