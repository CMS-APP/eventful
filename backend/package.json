{
  "name": "eventful-backend",
  "private": true,
  "scripts": {
    "deploy": "npm --prefix node run lint -- --max-warnings 0 && firebase deploy --only functions:node,functions:python,hosting",
    "deploy:node": "firebase deploy --only functions:node",
    "deploy:python": "firebase deploy --only functions:python",
    "deploy:hosting": "firebase deploy --only hosting"
  }
}
